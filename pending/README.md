# Pending SEO Change Manifests

Weekly manifests of new SEO tasks. Each file contains only the tasks
generated in that run — not the full backlog.

Full backlog and task status live in `seo_tasks.json` at the repo root.

Files are named `YYYY-MM-DD-changes.json` matching the report date.
